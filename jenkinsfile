pipeline {
    agent {
        node {
            label "linux"
        }
    }

    stages {

        stage ("Build") {
            steps {
                echo "Hello Build"
            }
        }

        stage ("Test") {
            steps {
                echo "Hello Test"
                sh "error"
            }
        }

        stage ("Deploy") {
            steps {
                echo "Hello Deploy"
            }
        }
    }

    post {
        always {
            echo "I will always say Hello again!"
        }
        success {
            echo "Build finished successfully!"
        }
        failure {
            echo "Build failed!"
        }
    }
}
